{
  "name": "steno",
  "version": "0.2.0",
  "private": true,
  "description": "Record and replay form fills instantly.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "jsdom": "^25.0.0"
  }
}
